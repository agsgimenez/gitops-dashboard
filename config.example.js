window.CI_CONFIG = {
  // List your repos here — grouped by org/owner.
  // The workspace switcher appears automatically when there are 2+ distinct owners.
  // Comments (// and #) are ignored.
  //
  // Your GitHub PAT is entered once in the UI and stored in browser localStorage.
  // It is never read from this file.
  repos: [

    // Org 1
    "my-org/repo-a",
    "my-org/repo-b",

    // Org 2
    "another-org/repo-c",

    // Personal
    "my-user/personal-repo",

  ]
};
